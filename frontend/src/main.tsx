import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'

import './styles.css'

// The UI is intentionally not implemented yet.
createRoot(document.getElementById('root')!).render(<StrictMode />)
